pipeline {
    agent any



    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run tests') {
            steps {
                sh 'make test'
            }
        }
    }

    post {
        always {
            echo 'Pipeline finished.'
        }
    }
}

