apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "helm.fullname" . }}-config
  labels:
    {{- include "helm.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.configMap }}
  {{ $key }}: "{{ $val }}"
  {{- end }}
