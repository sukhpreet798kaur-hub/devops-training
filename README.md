# My Project

## Quick start

1. Install dependencies (example for Python):
   ```bash
   pip install -r requirements.txt
   ```

2. Run tests:
   ```bash
   make test
   ```

3. Run the app:
   ```bash
   make run
   ```

4. Create a backup:
   ```bash
   make backup
   ```
